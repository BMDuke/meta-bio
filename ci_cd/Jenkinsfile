pipeline {
    agent any

    stages {
        stage('Build') {

            def metadata_api_image = docker.build("metadata_api", "../api")

            steps {
                echo 'Building..'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}