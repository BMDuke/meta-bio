node {

    checkout scm

    stage 'Building image...'
    def metadata_api_image = docker.build("metadata_api", "./api")


    stage 'Test image...'

    metadata_api_image.inside {

        shell 'pytest'

    }


    stage 'Deploy image...'

    

}
